syntax = "proto3";
package grpc.ready.service;

import "google/protobuf/timestamp.proto";
import "svc-storage-grpc-ready.proto";

// Storage service
service StorageRpc {
    // Simple call to check if the server is ready to accept connections
    rpc isReady (ReadyRequest) returns (ReadyResponse);
}
